# Configuración Nixpacks para Railway
# Incluye Chrome y ChromeDriver para Selenium

[phases.setup]
nixPkgs = ["python39", "chromium", "chromedriver"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 180 --workers 1"
